# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# env files — never commit. The anon key is public by design, but .env.local is
# still the wrong place for it to live in git. Use .env.local.example instead.
.env*
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*

# e2e screenshots
/.e2e-shots/
